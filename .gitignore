# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfvars
!*.tfvars.example
!terraform.tfvars.example

# Terraform state files - optional: some teams commit these
terraform.tfstate
terraform.tfstate.backup

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.auto.tfvars

# Include override files which are used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Temporary backup files
/tmp/iptables-backup-*
/tmp/terraform-iptables-backup-path.txt


# Terraform (added for terraform/ directory)
terraform/.terraform/
terraform/.terraform.tfstate.lock.info
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.auto.tfvars
terraform/override.tf
terraform/override.tf.json
terraform/*_override.tf
terraform/*_override.tf.json

# Temporary iptables backups
/tmp/iptables-backup-*
/tmp/terraform-iptables-backup-path.txt

# SSL Certificates - Include fullchain but exclude private key for security
ssl-*-privkey.pem
# ssl-*-fullchain.pem can be committed for transparency and deployment
